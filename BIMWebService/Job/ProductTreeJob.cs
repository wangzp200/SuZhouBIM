using System;
using BIMWebService.Mode;
using BIMWebService.Mode.Sys;

namespace BIMWebService.Job
{
   
    public class ProductTreeJob : BaseJob<ProductTree>
    {
        public override string Create(ProductTree data)
        {
            return "";
        }

        public override string Load(ProductTree data)
        {
          
            return "";
        }

        public override string Remove(ProductTree data)
        {
            return "";
        }

        public override string Update(ProductTree data)
        {
            return "";
        }
    }
}